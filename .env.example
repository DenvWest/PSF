# Site
NEXT_PUBLIC_SITE_URL=https://perfectsupplement.nl

# Contact / SMTP (kopieer naar .env.local — .env.local niet committen)
# Placeholders vervangen; zet CONTACT_SMTP_DISABLED=false om echte mail te sturen.
CONTACT_SMTP_DISABLED=false
OUTBOX_EMAIL=youroutboxemail@address.com
OUTBOX_EMAIL_PASSWORD=yourpassword
INBOX_EMAIL=yourinboxemail@address.com
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
